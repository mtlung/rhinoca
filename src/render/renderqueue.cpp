#include "pch.h"
#include "renderqueue.h"

/*

with current transform: drawImage(texture, pos...) -> DrawImageCmd

struct DrawImageCmd {
	texture, global order in current render frame, 4 vertex
}

DrawImageCmd ->

*/
namespace Render {

}	// Render
